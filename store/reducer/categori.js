const initialState = {
    categori:[]
}


const CategoriReducer = (state = initialState,action) =>{
    switch (action.type) {
        case "TES":
            return state
        default:
            return state
    }
}

export default CategoriReducer