const {SPECIFICATION} = require("../../data/dummy-data")

const initialState = {
    specification:SPECIFICATION
}

const specificationReducer = (state=initialState,action) =>{
    return state
}

export default specificationReducer